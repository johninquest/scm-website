---
// Detect browser language and redirect accordingly
const acceptLang = Astro.request.headers.get('accept-language') || '';
const prefersGerman = acceptLang.startsWith('de');
return Astro.redirect(prefersGerman ? '/de/' : '/en/');
---