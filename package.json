{
  "name": "scm-website",
  "type": "module",
  "version": "0.6.18",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro", 
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^5.14.5"
  }
}